# task test parameters
task_count = 40
users = [1, 2, 3]  # make sure id's are in the database

# user tests parameters
user_count = 20

# authorization parameters
auth_user = ("example", "PA55W0rd")  # (username, password) tuple, make sure the user exists in the database
